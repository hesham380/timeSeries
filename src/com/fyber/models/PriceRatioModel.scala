package com.fyber.models

/**
 * @author hesham380
 * class which represents rows read from input file and that will be added to OutputModel
 * Integer numOfSeconds -> timestamp of a measurement in seconds
 * Double priceRatioMeasurement -> measurement of price ratio
 */
class PriceRatioModel(var numOfSeconds: Int , var priceRatioMeasurement: Double) {
  
  
}